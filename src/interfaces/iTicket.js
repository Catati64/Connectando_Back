class iTicket {
    /* 
    Obtener la información de los boletos
        @return {promise<Ticket[]>}
        @throws {error} si hay un error en la obtención 
    */
    static async getTickets() { }
}

module.exports = iTicket
